# PLACEHOLDER — do not apply until CN2 S3 credentials exist.
# Example: create secret then patch Cluster sequoia-pg.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: sequoia-pg-backup-creds
#   namespace: sequoia-x
# type: Opaque
# stringData:
#   ACCESS_KEY_ID: "REPLACE_ME"
#   ACCESS_SECRET_KEY: "REPLACE_ME"
#
# Then add to Cluster.spec (see cnpg-cluster.yaml commented block):
#   backup:
#     barmanObjectStore:
#       destinationPath: s3://YOUR_CN2_BUCKET/sequoia-pg
#       endpointURL: https://YOUR_CN2_S3_ENDPOINT
#       s3Credentials:
#         accessKeyId:
#           name: sequoia-pg-backup-creds
#           key: ACCESS_KEY_ID
#         secretAccessKey:
#           name: sequoia-pg-backup-creds
#           key: ACCESS_SECRET_KEY
#       wal:
#         compression: gzip
#       data:
#         compression: gzip
#     retentionPolicy: "7d"
